/**
 * Logica interactiva para vista productos-create (admin) - Tienda
 */

function handleCreateProduct(e) {
      e.preventDefault();
      alert('¡Producto publicado exitosamente en la tienda!');
      window.location.href = 'productos-list.html';
    }