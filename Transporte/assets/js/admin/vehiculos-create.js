/**
 * Logica interactiva para vista vehiculos-create (admin) - Transporte
 */

function handleCreateVehicle(e) {
      e.preventDefault();
      alert('¡Vehículo registrado exitosamente en la flota activa!');
      window.location.href = 'vehiculos-list.html';
    }